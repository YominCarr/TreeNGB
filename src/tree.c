#include <stdio.h>
#include "tree.h"

void buildTree() {
    fprintf(stderr, "Implement 'buildTree'\n");
}

int findNGB(const int ipart, const float hsml, int *ngblist) {
    fprintf(stderr, "Implement 'findNGB'\n");
    return 0;
}
